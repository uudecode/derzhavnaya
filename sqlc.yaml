version: "2"
sql:
  - schema: "schema/migrations" # sqlc читает ваши .sql файлы миграций, чтобы понять схему!
    queries: "schema/queries.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"